<Window x:Class="pinvoke.wpfuiapp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="450" 
        Width="800"
        DataContext="{DynamicResource MyMainViewModel}">
    <StackPanel HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Orientation="Vertical">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="30" Text="Name"></TextBlock>
                <TextBlock FontSize="30" Text="{Binding Name,   UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="30" Text="PPM"></TextBlock>
                <TextBlock FontSize="30" Text="{Binding PPM,    UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Window>
